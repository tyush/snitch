// note that this is clearly not actual js, and just exists as a test file for snitch
// todo: make this compile

// todoooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooo: fix capitalization (pass)

// todo: add another m to hmmm (pass)
const hmmm = (yes) => { this.clearly() ? isValid() : javascript(yes) };

class ToDo {
    constructor(deez) {
        deez.nuts()
        this.status = "gottem"
    }
}